{
  "name": "@mtg/discord",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc --env development",
    "build": "wrangler build --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc --env production",
    "register:commands": "bun run src/registerCommands.ts",
    "register:commands:global": "bun --env-file=.env.production run src/registerCommands.ts --global",
    "test": "vitest run",
    "test:smoke": "vitest run src/__tests__/discordRoomCommand.integration.test.ts -t TO-01",
    "cf:typegen": "wrangler types",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mtg/shared": "workspace:*",
    "discord-api-types": "^0.38.34",
    "discord-interactions": "^4.4.0",
    "hono": "^4.10.4",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5",
    "wrangler": "^4.61.0"
  }
}
